<li>
  <%= gravatar_for user, size: Settings.user.size_image %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.user.delete_linkto"), user, method: :delete,
      data: {confirm: t("users.user.sure")} %>
  <% end %>
</li>
